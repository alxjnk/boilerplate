// import { selectBookingsContainerDomain } from '../selectors';

describe('selectBookingsContainerDomain', () => {
	it('Expect to have unit tests specified', () => {
		expect(true).toEqual(false);
	});
});
